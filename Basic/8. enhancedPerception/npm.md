npm -v      check version
npm init    initalize npm
then we got package.json
npm install [packageName]
package.json add new dependencies, node_modules/packageName
`
"dependencies": {
    "packageName": "packageVerison"
}
`